<!doctype html>
<html>

<head>
    <title>Authentication</title>
    <!-- load bootstrap css -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <!-- load fontawesome -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        body {
            padding-top: 80px;
        }
    </style>
</head>

<body>
    <div class="container">

        <div class="jumbotron text-center">
            <h1><span class="fa fa-lock"></span> Node Authentication</h1>

            <p>Login or Register with:</p>
            
            <a href="/profile" class="btn btn-default"><span class="fa fa-user"></span>Profile</a>
            <a href="/login" class="btn btn-default"><span class="fa fa-user"></span> Local Login</a>
            <a href="/signup" class="btn btn-default"><span class="fa fa-user"></span> Local Signup</a>            
        </div>
        
        <div class="row">

            <!-- LOCAL INFORMATION -->
            <% post.forEach(function(doc){ %>
   
            <div class="col-sm-6">
                <div class="well">
                    <h3><span class="fa fa-user"></span> <%= doc.Name %> </h3>
                      <p>
                        <strong>Owner</strong>:
                        <%= doc.Owner %><br>
                            <strong>Type</strong>:
                            <%= doc.Type %><br>
                                <strong>Require</strong>:
                                <%= doc.Require %><br>
                                    <strong>Place</strong>:
                                    <%= doc.Place %><br>
                                        <strong>Time</strong>:
                                        <%= doc.Time %><br>
                                            <strong>Money</strong>:
                                            <%= doc.Money %><br>
                                                <strong>People</strong>:
                                                <%= doc.People %><br>
                                                    <strong>Detail</strong>:
                                                    <%= doc.Detail %><br>
                                
                     </p>
                     <button type="submit" class="btn btn-danger">Like</button>
                     <a href=  '/detail/<%= doc._id %>' class="btn btn-primary">Detail</a>
                     <a href=  '/applyjob/<%= doc._id %>' class="btn btn-primary">Apply Now</a>                    
                </div>
            </div>
            <% }) %>  
        </div>


    </div>
</body>

</html>